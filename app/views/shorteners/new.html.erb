<h1>New shortener</h1>
<%= form_for(@shortener) do |f| %>
  <% if @shortener.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shortener.errors.count, "error") %> prohibited this shortener from being saved:</h2>

      <ul>
      <% @shortener.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :orig_url %><br />
    <%= f.text_field :orig_url, placeholder: 'http://exaple.com' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', shorteners_path %>
